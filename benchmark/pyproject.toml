[project]
name = "benchmark"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "adbc-driver-manager>=1.9.0",
    "confluent-kafka>=2.12.2",
    "deltalake",
    "dotenv>=0.9.9",
    "duckdb>=1.4.3",
    "pandas>=2.3.3",
    "pyarrow>=22.0.0",
    "pyiceberg[pyarrow,sql-postgres,sql-sqlite]>=0.10.0",
    "pyspark>=4.0.1",
]

[tool.uv.sources]
deltalake = { path = "deltalake-1.3.0-cp310-abi3-manylinux_2_39_x86_64.whl" }
